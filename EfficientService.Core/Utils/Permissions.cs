namespace EfficientService.Core.Utils
{
    /// <summary>
    /// Present a permission set for every user role
    /// </summary>
    public enum Permissions
    {
        NoPermission = 0,
        Client = 1,
        Executor = 2,
        Admin = 3
    }
}
